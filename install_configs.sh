# This will act as the central install script
# Then it will fetch and run the proper script
# Try to keep this loosely coupled as it should dynamically adjust
# Each install script will be associated with its own directory
# The scripts must start with 'install_' and ideal follow with directory name
# Allow it to run install_builder

REPO_URL="https://github.com/HarrisonStokes/personal-configs"

